import { redeChart } from '../../shared/rede';

export default redeChart({
  label:
    'Rede não-direcionada entre doze nós identificados por letra, com as conexões ' +
    'desenhadas como linhas retas e os nós posicionados por um algoritmo de forças.',
  aspectRatio: 7 / 6.5,
});
