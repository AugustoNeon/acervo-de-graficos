import { redeChart } from '../../shared/rede';

export default redeChart({
  label:
    'Rede direcionada e ponderada de rotas entre oito cidades brasileiras. A seta indica o ' +
    'sentido da rota e a espessura da linha indica o volume que passa por ela.',
  aspectRatio: 8 / 6.5,
});
