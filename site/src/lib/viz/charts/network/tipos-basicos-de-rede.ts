import { redeChart } from '../../shared/rede';

export default redeChart({
  label:
    'Dois painéis com a mesma rede de oito pessoas nas mesmas posições. No primeiro, todas ' +
    'as conexões têm a mesma espessura, indicando só a presença do vínculo; no segundo, a ' +
    'espessura da linha indica a intensidade de cada vínculo.',
  aspectRatio: 10 / 5,
});
