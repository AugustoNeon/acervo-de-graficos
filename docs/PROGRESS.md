# Progresso

> **Escopo**: log de gráficos já adicionados + histórico do projeto. **Leia se**: quiser saber o estado atual antes de continuar. **Não use para**: decisões de arquitetura do projeto (ver "Decisões fechadas" em [`AGENTS.md`](../AGENTS.md)).

Log de todos os gráficos adicionados ao projeto. Atualize sempre que um gráfico novo for concluído (ver [`WORKFLOW.md`](WORKFLOW.md), passo 6).

| Data       | Categoria | Gráfico | Pasta | Fonte |
|------------|-----------|---------|-------|-------|
| 2026-08-20 | map | Mapa de conexões: intercâmbio de pesquisadores (D3) | [graficos/map/mapa-conexoes-pesquisa](../graficos/map/mapa-conexoes-pesquisa) | [r-graph-gallery.com/connecting-routes](https://r-graph-gallery.com/how-to-draw-connecting-routes-on-map-with-r-and-great-circles.html) |
| 2026-08-20 | correlation | Correlograma: indicadores municipais (D3) | [graficos/correlation/correlograma-indicadores](../graficos/correlation/correlograma-indicadores) | [r-graph-gallery.com/correlogram](https://r-graph-gallery.com/correlogram.html) |
| 2026-08-20 | part-of-whole | Treemap com zoom: orçamento municipal (D3) | [graficos/part-of-whole/treemap-orcamento-municipal](../graficos/part-of-whole/treemap-orcamento-municipal) | [r-graph-gallery.com/235](https://r-graph-gallery.com/235-treemap-with-subgroups.html) |
| 2026-08-20 | distribution | Histograma: largura de bin variável (D3) | [graficos/distribution/histograma-largura-de-bin](../graficos/distribution/histograma-largura-de-bin) | [r-graph-gallery.com/220](https://r-graph-gallery.com/220-basic-ggplot2-histogram.html) |
| 2026-08-20 | distribution | Violino + boxplot: três variações (D3) | [graficos/distribution/violino-e-boxplot](../graficos/distribution/violino-e-boxplot) | [r-graph-gallery.com/violin](https://r-graph-gallery.com/violin.html) |
| 2026-08-20 | distribution | Boxplot clássico: cinco variações (D3) | [graficos/distribution/boxplot-classico](../graficos/distribution/boxplot-classico) | [r-graph-gallery.com/262](https://r-graph-gallery.com/262-basic-boxplot-with-ggplot2.html) |
| 2026-08-20 | flow | Diagrama de cordas: transferências entre clubes (D3) | [graficos/flow/chord-transferencias-clubes](../graficos/flow/chord-transferencias-clubes) | [r-graph-gallery.com/123](https://r-graph-gallery.com/123-circular-plot-circlize-package-2.html) |
| 2026-08-18 | distribution | Ridgeline plot: avaliações por bairro (KDE em D3) | [graficos/distribution/ridgeline-avaliacoes-bairros](../graficos/distribution/ridgeline-avaliacoes-bairros) | [r-graph-gallery.com/294](https://r-graph-gallery.com/294-basic-ridgeline-plot.html) |
| 2026-08-18 | part-of-whole | Barplot agrupado e empilhado (switcher de 3 estados, D3) | [graficos/part-of-whole/barplot-agrupado-empilhado](../graficos/part-of-whole/barplot-agrupado-empilhado) | [r-graph-gallery.com/barplot](https://r-graph-gallery.com/barplot.html) |
| 2026-08-18 | ranking | Lollipop chart com detalhamento por plataforma (D3) | [graficos/ranking/lollipop-streaming](../graficos/ranking/lollipop-streaming) | [r-graph-gallery.com/lollipop-plot](https://r-graph-gallery.com/lollipop-plot.html) |
| 2026-08-18 | ranking | Barplot clássico: cinco variações (switcher animado, D3) | [graficos/ranking/barplot-classico](../graficos/ranking/barplot-classico) | [r-graph-gallery.com/barplot](https://r-graph-gallery.com/barplot.html) |
| 2026-08-18 | evolution | Bolhas animadas estilo Gapminder (D3) | [graficos/evolution/bolhas-animadas-planetas](../graficos/evolution/bolhas-animadas-planetas) | [r-graph-gallery.com/271](https://r-graph-gallery.com/271-ggplot2-animated-gif-chart-with-gganimate.html) |
| 2026-08-18 | correlation | Superfície 3D de interação (rgl) | [graficos/correlation/superficie-3d-interacao](../graficos/correlation/superficie-3d-interacao) | [r-graph-gallery.com/167](https://r-graph-gallery.com/167-animated-3d-plot-imagemagick.html) |
| 2026-08-18 | ranking | Radar chart com múltiplos grupos (fmsb + D3) | [graficos/ranking/radar-multiplos-grupos](../graficos/ranking/radar-multiplos-grupos) | [r-graph-gallery.com/143](https://r-graph-gallery.com/143-spider-chart-with-saveral-individuals.html) |
| 2026-07-30 | part-of-whole | Dendrograma interativo colapsável (collapsibleTree) | [graficos/part-of-whole/dendrograma-interativo](../graficos/part-of-whole/dendrograma-interativo) | [r-graph-gallery.com/336](https://r-graph-gallery.com/336-interactive-dendrogram-with-collapsibletree.html) |
| 2026-07-30 | ranking | Circular barplot empilhado (ggplot2) | [graficos/ranking/circular-barplot-empilhado](../graficos/ranking/circular-barplot-empilhado) | [r-graph-gallery.com/299](https://r-graph-gallery.com/299-circular-stacked-barplot.html) |
| 2026-07-29 | part-of-whole | Circle packing hierárquico (ggraph + circlepackeR) | [graficos/part-of-whole/circle-packing-hierarquico](../graficos/part-of-whole/circle-packing-hierarquico) | [r-graph-gallery.com/315](https://r-graph-gallery.com/315-hide-first-level-in-circle-packing.html) |
| 2026-07-29 | part-of-whole | Circle packing simples (packcircles + ggiraph) | [graficos/part-of-whole/circle-packing-simples](../graficos/part-of-whole/circle-packing-simples) | [r-graph-gallery.com/307](https://r-graph-gallery.com/307-add-space-in-circle-packing.html) |
| 2026-07-27 | part-of-whole | Rosca de alocação de tempo (ggplot2 + plotly) | [graficos/part-of-whole/rosca-alocacao-tempo](../graficos/part-of-whole/rosca-alocacao-tempo) | [r-graph-gallery.com/doughnut-plot](https://r-graph-gallery.com/doughnut-plot.html) |
| 2026-07-27 | correlation | Dispersão 3D de cafés especiais (rgl) | [graficos/correlation/dispersao-3d-cafes](../graficos/correlation/dispersao-3d-cafes) | [r-graph-gallery.com/3d_scatter_plot](https://r-graph-gallery.com/3d_scatter_plot.html) |
| 2026-07-27 | map       | Dashboard interativo: mapa + dispersão + barras (ggiraph) | [graficos/map/dashboard-inovacao-ggiraph](../graficos/map/dashboard-inovacao-ggiraph) | [r-graph-gallery.com/414](https://r-graph-gallery.com/414-map-multiple-charts-in-ggiraph.html) |
| 2026-07-27 | map       | Bubble map interativo do Brasil (leaflet) | [graficos/map/bubble-map-leaflet-brasil](../graficos/map/bubble-map-leaflet-brasil) | [r-graph-gallery.com/19](https://r-graph-gallery.com/19-map-leafletr.html) |
| 2026-07-24 | evolution | Série temporal interativa customizada (dygraphs) | [graficos/evolution/serie-temporal-customizada-dygraphs](../graficos/evolution/serie-temporal-customizada-dygraphs) | [r-graph-gallery.com/318](https://r-graph-gallery.com/318-custom-dygraphs-time-series-example.html) |
| 2026-07-24 | evolution | Linha interativa com CSS customizado (ggiraph) | [graficos/evolution/linha-interativa-ggiraph-css](../graficos/evolution/linha-interativa-ggiraph-css) | [r-graph-gallery.com/412](https://r-graph-gallery.com/412-customize-css-in-interactive-ggiraph.html) |
| 2026-07-24 | network   | Rede direcionada e ponderada (visNetwork) | [graficos/network/rede-direcionada-ponderada](../graficos/network/rede-direcionada-ponderada) | [data-to-viz.com/graph/network](https://www.data-to-viz.com/graph/network.html) |
| 2026-07-24 | network   | Rede densa (hairball, visNetwork) | [graficos/network/rede-densa-hairball](../graficos/network/rede-densa-hairball) | [data-to-viz.com/graph/network](https://www.data-to-viz.com/graph/network.html) |
| 2026-07-24 | network   | Comparação de layouts (Fruchterman-Reingold, DrL, Aleatório) | [graficos/network/comparacao-layouts](../graficos/network/comparacao-layouts) | [data-to-viz.com/graph/network](https://www.data-to-viz.com/graph/network.html) |
| 2026-07-24 | network   | Tipos básicos de rede (não-ponderada vs ponderada) | [graficos/network/tipos-basicos-de-rede](../graficos/network/tipos-basicos-de-rede) | [data-to-viz.com/graph/network](https://www.data-to-viz.com/graph/network.html) |
| 2026-07-23 | network   | Arc diagram (versão interativa em D3.js puro) | [graficos/network/arc-diagram-d3](../graficos/network/arc-diagram-d3) | [data-to-viz.com/graph/arc](https://www.data-to-viz.com/graph/arc.html) |
| 2026-07-23 | distribution | Superfície 3D de densidade (plotly + MASS::kde2d) | [graficos/distribution/superficie-3d-densidade](../graficos/distribution/superficie-3d-densidade) | [data-to-viz.com/graph/density2d](https://www.data-to-viz.com/graph/density2d.html) |
| 2026-07-23 | distribution | Densidade 2D (stat_density_2d) | [graficos/distribution/densidade-2d-contorno](../graficos/distribution/densidade-2d-contorno) | [data-to-viz.com/graph/density2d](https://www.data-to-viz.com/graph/density2d.html) |
| 2026-07-22 | correlation | Heatmap com clustering hierárquico (heatmaply) | [graficos/correlation/heatmap-clustering-heatmaply](../graficos/correlation/heatmap-clustering-heatmaply) | [data-to-viz.com/graph/heatmap](https://www.data-to-viz.com/graph/heatmap.html) |
| 2026-07-22 | flow      | Sankey diagram simplificado (networkD3) | [graficos/flow/sankey-networkd3-simplificado](../graficos/flow/sankey-networkd3-simplificado) | [r-graph-gallery.com/323](https://r-graph-gallery.com/323-sankey-diagram-with-the-networkd3-library.html) |
| 2026-07-22 | network   | Rede interativa com networkD3 (simpleNetwork) | [graficos/network/rede-interativa-networkd3](../graficos/network/rede-interativa-networkd3) | [r-graph-gallery.com/network-interactive](https://r-graph-gallery.com/network-interactive.html) |
| 2026-07-22 | evolution | Streamgraph interativo com legenda/dropdown | [graficos/evolution/streamgraph-legenda-interativo](../graficos/evolution/streamgraph-legenda-interativo) | [r-graph-gallery.com/156](https://r-graph-gallery.com/156-interactive-streamgraph-with-legend.html) |
| 2026-07-21 | network   | Hierarchical Edge Bundling com labels, cores e tamanhos | [graficos/network/hierarchical-edge-bundling-labels](../graficos/network/hierarchical-edge-bundling-labels) | [r-graph-gallery.com/311](https://r-graph-gallery.com/311-add-labels-to-hierarchical-edge-bundling.html) |

## Histórico do projeto

- **2026-08-20**: trigésimo sexto gráfico adicionado (mapa de conexões, `sf`+`geosphere`), a partir de [r-graph-gallery.com/connecting-routes](https://r-graph-gallery.com/how-to-draw-connecting-routes-on-map-with-r-and-great-circles.html) — escolhido por ser a lacuna mais óbvia de `map`, empatada com `flow` como categoria mais rasa do acervo (só 2 gráficos cada antes desta sessão): os dois mapas existentes mostram pontos isolados (bubble map) ou países pintados (dashboard coroplético), nenhum mostra CONEXÃO entre lugares. Dados fictícios: rede de intercâmbio de pesquisadores entre 10 centros de pesquisa em cidades reais (coordenadas verdadeiras), 17 rotas com número fictício de pesquisadores/ano (`set.seed(2417)`). Paleta qualitativa `RColorBrewer::brewer.pal(4,"Accent")` por região de origem, ainda não usada em nenhum outro gráfico do acervo. **Refatoração**: extraída `shared/mapa.ts` (`corrigirEnrolamento`, a correção de sentido de rotação de anel do GeoJSON exportado por `sf::st_write()`) a partir do `dashboard-inovacao-ggiraph.ts`, que tinha a função local — mesmo padrão de extração no segundo uso já seguido por `shared/densidade.ts`; dashboard reverificado no navegador depois da extração, sem regressão. **Achado real de R, generalizável**: `geosphere::gcIntermediate(p1, p2, breakAtDateLine = TRUE)` devolve uma matriz de pontos quando a rota não cruza o antimeridiano, mas uma LISTA de matrizes quando cruza — código que assume sempre-matriz quebra silenciosamente (ou desenha uma rota absurda cruzando o mapa inteiro) na primeira rota que cruzar o Pacífico; a correção é sempre normalizar pra lista (`if (is.list(gc)) gc else list(gc)`) antes de iterar. **Achado real, generalizável pra qualquer export futuro**: `jsonlite::write_json()` descarta os NOMES de um vetor nomeado (`setNames(cores, regioes)`) quando ele é passado direto dentro de uma lista — vira um array JSON simples, perdendo a associação região↔cor; `as.list()` no vetor nomeado antes de exportar preserva os nomes como chaves do objeto JSON. **Achado de dado, não de código**: a primeira versão da tabela de rotas tinha 2 pares duplicados nos dois sentidos por engano (Nova York↔Cidade do México, Tóquio↔Sydney), desenhando duas curvas sobrepostas da mesma cor — pego comparando visualmente o `output.png` antes de aceitar como pronto, não por erro do R.

- **2026-08-20**: trigésimo quinto gráfico adicionado (correlograma, `ggcorrplot`), a partir de [r-graph-gallery.com/correlogram](https://r-graph-gallery.com/correlogram.html) — escolhido por ser a lacuna mais óbvia de `correlation`: a categoria só tinha entradas "exóticas" (heatmap com clustering, dois gráficos 3D), nenhum correlograma clássico de matriz de correlação. Dados fictícios: 8 indicadores socioeconômicos de 150 municípios fictícios (`set.seed(6203)`), construídos a partir de um fator latente comum de "desenvolvimento" com ruído próprio por variável, deliberadamente incluindo uma variável (tempo de deslocamento) quase independente do fator latente pra gerar correlações não significativas de verdade na matriz, não só as fortes. Paleta divergente `RColorBrewer::brewer.pal(3,"PuOr")`, ainda não usada em nenhum outro gráfico do acervo. **Achado real, generalizável**: a ordenação por semelhança que `ggcorrplot(hc.order = TRUE)` aplica (`hclust(as.dist(1 - matriz_corr))`) foi exportada pronta no `data.json` (`ordem`) em vez de o D3 recalcular clustering — ao contrário do layout de rede/treemap (estocástico ou caro de exportar), aqui a ordem é barata e determinística de gerar no R, então reexportar a decisão pronta evita duas implementações do mesmo cálculo divergindo. A versão interativa desenha só o triângulo superior (mesmo recorte do `output.png`), com realce ligado: focar uma célula destaca todas as outras que compartilham uma das duas variáveis, mais as próprias etiquetas de linha/coluna — generaliza o mesmo princípio de "seguir uma chave por toda a estrutura" já usado no heatmap de clustering, aplicado a compartilhar uma variável em vez de uma posição de linha/coluna fixa.

- **2026-08-20**: trigésimo quarto gráfico adicionado (treemap com zoom, `treemapify::geom_treemap()`), a partir de [r-graph-gallery.com/235](https://r-graph-gallery.com/235-treemap-with-subgroups.html) — fecha a lista de prioridades levantada no início da sessão (`part-of-whole` era a única categoria com hierarquia grande faltando: já tinha circle packing ×2 e dendrograma, mas nenhum treemap). Instalado `treemapify` (não estava no ambiente). Dados fictícios: orçamento anual de uma prefeitura fictícia, secretaria > programa > ação (6 secretarias, 2 programas cada, 2–3 ações cada, valores de 0,9 a 28,4 milhões) — faixa de valores deliberadamente larga pra mostrar a força do treemap em comparar itens de escalas bem diferentes de uma vez. **Achado real de D3, o mais generalizável da sessão**: layout do treemap calculado UMA VEZ SÓ pra árvore inteira (todos os 3 níveis já nascem com coordenadas aninhadas); "zoom" não recalcula layout nenhum — são só duas `scaleLinear` (x/y) cujo domínio muda pro retângulo do nó em foco a cada clique, e como os filhos já têm coordenadas DENTRO do pai (por construção), reescalar o domínio faz eles se espalharem pra preencher a tela sozinhos. Evita de propósito o approach ingênuo de zoom via `transform: scale()` num `<g>`, que distorceria espessura de traço e tamanho de fonte junto com o zoom — a técnica de "reescalar o domínio de uma escala em vez de aplicar transform" vale pra qualquer zoom futuro neste acervo (mapas, redes grandes). Rótulos usam `<clipPath>` por bloco (corta texto comprido na borda do retângulo em vez de vazar pro vizinho) e um gate de tamanho mínimo (blocos pequenos demais simplesmente não mostram texto, só tooltip). Breadcrumb reaproveita a classe `.viz-controles` (mesma dos botões de estado dos outros switchers) só re-populando os botões a cada nível — nenhum CSS novo precisou ser escrito.

- **2026-08-20**: trigésimo terceiro gráfico adicionado (histograma com slider de número de bins), a partir de [r-graph-gallery.com/220](https://r-graph-gallery.com/220-basic-ggplot2-histogram.html) — terceira e última página planejada da leva de `distribution` desta sessão (depois do [boxplot clássico](../graficos/distribution/boxplot-classico) e do [violino](../graficos/distribution/violino-e-boxplot)), fechando a lacuna que restava: nenhum histograma clássico no acervo. Dados fictícios: idade de 300 participantes de uma corrida de bairro fictícia (`set.seed(918)`), misturando categoria infantil (90, 6–16 anos) e adulta (210, 18–70 anos) — terceira vez na fileira que a distribuição é construída bimodal de propósito (mesmo fio condutor do violino), agora pra mostrar que a largura do bin, não só a técnica de gráfico, também pode esconder ou revelar estrutura. **Diferença de padrão**: primeiro switcher do acervo controlado por um slider contínuo (`input[type=range]`, classe `.viz-slider` já usada no [contorno 2D](../graficos/distribution/densidade-2d-contorno)) em vez de botões de estado discretos — e também o primeiro sem chave estável entre posições: como o número de bins muda a cada arrasto, não existe "a mesma barra mudando de forma" pra preservar (diferente do path de 10 pontos do boxplot ou da borda travada do violino), então as barras usam enter/exit comum do D3, indexadas por posição. **Terceiro uso de `shared/densidade.ts`**: a curva de KDE por cima do histograma é calculada uma vez só (não muda com o slider), mas sua ALTURA é reescalada a cada arrasto (`densidade × n × largura do bin atual`) pra continuar na mesma unidade de contagem do eixo Y, que também muda de escala a cada posição — mostra visualmente que o histograma é uma discretização com perda da mesma curva contínua.

- **2026-08-20**: trigésimo segundo gráfico adicionado (violino + boxplot, `geom_violin()` + `ggdist::stat_halfeye()`), a partir de [r-graph-gallery.com/violin](https://r-graph-gallery.com/violin.html) — segunda página da família de `distribution` iniciada pelo [boxplot clássico](../graficos/distribution/boxplot-classico), completando o par que faltava. Dados fictícios: velocidade de download (Mbps) de 5 provedores de internet fictícios (`set.seed(2609)`), com um deles ("ConectaSul") gerado de propósito como mistura de duas distribuições (DSL + fibra) — bimodal, o caso clássico que motiva "por que violino além de boxplot": a variação "violino + caixa" mostra a mesma caixa que o boxplot clássico desenharia sozinha, mas agora ao lado da forma completa que revela os dois picos escondidos dentro dela. **Refatoração**: extraído `shared/densidade.ts` (KDE gaussiano, largura de Silverman) do [ridgeline](../graficos/distribution/ridgeline-avaliacoes-bairros), que tinha a função local — mesmo motivo de extração já registrado pra `shared/cartesiano.ts`, agora com uma segunda técnica repetida entre gráficos. Ridgeline re-testado no navegador depois da refatoração, sem regressão. **Achado real de D3, generalizável**: a curva do violino é sempre o mesmo `<path>` de área (`d3.area().x0().x1().y()`) nos 3 estados — no "violino"/"violino + caixa" as duas bordas se afastam do eixo central pela densidade; no "meio violino" a borda esquerda é travada numa CONSTANTE (o centro) em vez de uma função da densidade, e só a direita continua curvando. Mesmo princípio do path de 10 pontos do [boxplot clássico](../graficos/distribution/boxplot-classico) (chave estável, nunca cria/destrói elemento), agora aplicado a uma curva de densidade em vez de um polígono. **Achado real de TypeScript**: passar `numero | funcao` (dependendo do estado) direto pro `.x0()` do `d3.area()` quebra a checagem de tipos — os tipos do `d3-shape` declaram duas sobrecargas separadas (uma pra número, outra pra função) e o TS não escolhe automaticamente a partir de um argumento com tipo união; a correção é sempre passar a forma de função, com o `if`/ternário de estado DENTRO do corpo dela. **Achado de ambiente**: `gghalves` (pacote mais citado pra meio-violino) não tinha build disponível pra esta versão do R no CRAN; `ggdist::stat_halfeye()` cobriu o mesmo caso de uso e instalou sem problema — checar `ggdist` primeiro da próxima vez.

- **2026-08-20**: trigésimo primeiro gráfico adicionado (boxplot clássico, `ggplot2::geom_boxplot()`), a partir de [r-graph-gallery.com/262](https://r-graph-gallery.com/262-basic-boxplot-with-ggplot2.html) — escolhido por ser a lacuna mais óbvia de `distribution`: a categoria já tinha ridgeline, densidade 2D e superfície 3D (todos "exóticos"), mas nenhum dos clássicos univariados (boxplot/violin/histograma). Mesmo molde de "família de 5 variações com switcher" já usado no [barplot clássico](../graficos/ranking/barplot-classico), aplicado às opções nativas do próprio `geom_boxplot()` (jitter, `notch`, `varwidth`) em vez de reinventar variações. Dados fictícios: tempo de resolução de chamados (horas) de 6 equipes de suporte fictícias via `rlnorm()` (`set.seed(7331)`) — distribuição de cauda longa, gera outliers de verdade. Paleta `RColorBrewer::brewer.pal(6,"Set1")`, ainda não usada em nenhum outro gráfico do acervo. **Achado real de D3, generalizável**: a caixa é desenhada como um path SVG de 10 pontos fixos (não um `<rect>`) justamente pra poder "morphar" suavemente entre retângulo comum e entalhe (notch) trocando só a posição X de 2 dos 10 pontos — mesma ideia de "chave estável nunca cria/destrói elemento" já usada no resto do runtime D3 do site, aplicada dentro de uma única forma em vez de entre elementos. Formula de McGill (1978) pro entalhe (`1.58×IQR/√n`) deixada **sem** clamping: quando a amostra é pequena, o entalhe estoura pra fora dos limites Q1/Q3 de propósito — é o mesmo aviso real (`Notch went outside hinges`) que o `ggplot2` também solta ao gerar o `output.png`, preservado como sinal estatístico genuíno em vez de escondido. **Achado real de evento, novo neste gráfico** (nenhum gráfico anterior do acervo tinha elemento interativo aninhado dentro de outro elemento interativo): pontos individuais (outliers/jitter) moram dentro do mesmo `<g>` que already tem seus próprios listeners de hover — `pointermove` **borbulha** (ao contrário de `pointerenter`/`pointerleave`), então passar o cursor num ponto disparava o listener do ponto E do grupo pai na mesma jogada, e o do pai rodava por último e sobrescrevia o tooltip; corrigido com `event.stopPropagation()` no listener do ponto. Vale conferir em qualquer gráfico D3 futuro que aninhe um elemento com hover próprio dentro de outro que também tem hover. **Nota de depuração, não é bug do gráfico**: ao testar manualmente clicando pelos 5 estados em sequência rápida, um clique pareceu "não fazer nada" (a caixa ficou com a geometria do estado anterior) — rastreado até o `ResizeObserver` do `mount.ts` disparando um redesenho completo (`canvas.replaceChildren()` + `aplicarEstado('basico', false)`) no meio de uma transição, resetando o estado. Confirmado que isso é comportamento **compartilhado e pré-existente** de todo gráfico com switcher deste acervo (o barplot clássico tem exatamente a mesma limitação) — não uma regressão deste gráfico, e não vale a pena corrigir isoladamente aqui.

- **2026-08-20**: trigésimo gráfico adicionado (diagrama de cordas, `circlize`), a partir de [r-graph-gallery.com/123](https://r-graph-gallery.com/123-circular-plot-circlize-package-2.html) — escolhido por ser a lacuna mais óbvia da categoria `flow` (só tinha o Sankey, que é sequencial/mão-única; faltava o caso de troca **mútua** entre as mesmas categorias). Dados fictícios: transferências de jogadores entre 6 clubes fictícios numa janela de mercado (`set.seed(4127)`), matriz assimétrica e esparsa (~60% dos pares com algum fluxo). Paleta `RColorBrewer::brewer.pal(6,"Paired")`, ainda não usada em nenhum outro gráfico do acervo. Achado de ambiente (não do gráfico em si): nesta máquina o R fica em `AppData\Local\Programs\R\R-4.6.1` (instalação por-usuário), não em `Program Files` — confirma de novo o aviso do [`SETUP.md`](SETUP.md) de sempre checar o caminho antes de assumir. Achado de `circlize`: o `circos.par()` padrão só reserva `canvas.xlim/ylim = c(-1,1)`, sem folga pro texto dos rótulos que sai pra fora do círculo — um nome comprido (`"Atlético Norte"`) saía cortado na borda do PNG até aumentar a folga explicitamente (`circos.xlim = c(-1.3,1.3)`) antes de chamar `chordDiagram()`. Achado de D3 (`d3-chord`, dentro do pacote `d3` principal desde a v6, sem dependência extra): existem dois geradores de layout com semântica de tamanho de arco bem diferente — `d3.chord()` (usado no `chord()` "não-direcionado") mede o comprimento do arco de cada grupo só pela soma da **linha** da matriz (saída), então um nó que só recebe (nunca envia) nasce com arco de comprimento zero e some do círculo; `d3.chordDirected()` mede pela soma de linha **e** coluna (entrada + saída) e ainda separa cada fluxo em uma fita própria (em vez de fundir os dois sentidos numa única fita de pontas diferentes, como o `circlize` faz por padrão) — foi o escolhido aqui exatamente por isso, e combinado com `d3.ribbonArrow()` pra desenhar a seta de verdade em cada fita. Vale conferir esse detalhe (`chord()` vs. `chordDirected()`) em qualquer diagrama de cordas futuro cujo dado tenha nós só-de-entrada.

- **2026-08-18**: vigésimo nono gráfico adicionado (ridgeline plot / joyplot, `ggridges`), a partir de [r-graph-gallery.com/294](https://r-graph-gallery.com/294-basic-ridgeline-plot.html) — a página tinha 3 variações progressivas de tutorial (básica, forma de histograma, gradiente de cor pela variável numérica), combinadas na versão mais completa: `geom_density_ridges_gradient()` colorindo cada ponto da curva pela própria nota (`after_stat(x)`), não por bairro. Escolhida por preencher a maior lacuna de categoria do acervo (`distribution` tinha só densidade 2D e superfície 3D, nenhum dos clássicos univariados). Dados fictícios: notas de avaliação (0–10) de oito bairros fictícios, cada um com média/desvio-padrão próprios (`set.seed(4127)`) pensados para formas bem diferentes entre si, no lugar do dataset real de temperatura em Lincoln do exemplo original. Paleta `RColorBrewer` divergente (`RdYlGn` via `scale_fill_distiller`) no lugar do `viridis` original. Primeira versão interativa deste gráfico — `ggridges` não expõe a curva de densidade calculada de um jeito reaproveitável, então o D3 estima sua própria densidade com kernel gaussiano e largura de banda de Silverman por bairro, e desenha o gradiente de cor com um `<linearGradient>` próprio alinhado à escala X (o `fill` de um `<path>` já pinta só dentro da própria forma, sem precisar de `clipPath`). As faixas são desenhadas de trás pra frente (maior mediana primeiro) pra reproduzir a sobreposição clássica do ridgeline. Achado de caminho: o hover foi escrito nos elementos `<g>` de cada faixa (não no `<path>` como nos gráficos anteriores desta sessão) — funcionou sem ajuste, mas a verificação inicial usou pontos de teste dentro do retângulo delimitador (`getBBox()`) da curva em vez de dentro da forma preenchida de fato, gerando um falso alarme de "hover não funciona"; a curva ocupa só uma fração do próprio retângulo delimitador perto do topo (onde ela afina rumo ao pico), então testar hover num gráfico de área precisa mirar um ponto perto da base (larga) da forma, não perto do topo do bounding box.

- **2026-08-18**: **vigésimo sexto a vigésimo oitavo gráficos** — "família do barplot" em três entradas, a pedido do usuário ("gostaria de ir para o gráfico clássico de barras... gostaria de algo grande"), depois de uma conversa explícita sobre como caber várias variações da família de barra sem virar uma página só sobrecarregada. Decisão de escopo: em vez de uma mega-página com 9 estados, três gráficos separados, cada um encaixando na categoria certa (não na classificação da fonte, que mistura tudo em "Barplot") — [barplot clássico](../graficos/ranking/barplot-classico) (ranking: básico/ordenado/horizontal/largura variável/erro, 5 estados) e [lollipop](../graficos/ranking/lollipop-streaming) (ranking: é comparação/ranking) versus [agrupado/empilhado/percentual](../graficos/part-of-whole/barplot-agrupado-empilhado) (part-of-whole: é composição de um total). Os três compartilham o mesmo dataset fictício (audição por gênero musical × plataforma de streaming, `set.seed(5817)` repetida em cada `script.R`) e se cross-linkam, lendo como uma família coesa vista de três ângulos. Pedido explícito do usuário nos três: cor por categoria (nunca uma cor só pra todas as barras) — géneros usam `RColorBrewer::brewer.pal(6,"Dark2")` (barplot clássico e lollipop), plataformas usam `brewer.pal(4,"Set2")` (agrupado/empilhado), paletas deliberadamente diferentes porque codificam dimensões diferentes. Todos os três reutilizam o princípio de "mesma chave, elementos nunca entram/saem" já estabelecido no acervo (rede/dendrograma) aplicado a **reorganização de layout inteira**: barplot clássico anima entre 5 arranjos diferentes (inclusive troca de eixo x/y no estado horizontal) sempre com as mesmas 6 chaves; agrupado/empilhado/percentual usa `d3.stack()` (com `stackOffsetExpand` pro percentual) recalculado no navegador a partir da célula bruta, nunca importando posição empilhada pronta do R. **Achado real, generalizável pro resto do runtime D3 do site**: hover que anima um atributo (crescer uma barra, opacidade de realce) e a troca de estado que anima outro atributo do MESMO elemento colidem se ambos usarem `selection.transition()` sem nome — D3 rastreia transições por par (elemento, nome), e uma segunda chamada sem nome **cancela** a primeira mesmo mexendo em atributos diferentes, congelando o elemento a meio caminho da transição anterior (detectado ao clicar num botão de estado e passar o cursor logo em seguida — sintoma: barra/ponto trava em posição/tamanho errado até a próxima transição completa). Corrigido dando nomes distintos (`.transition('hover')` vs. a transição de estado sem nome) nos três gráficos — vale aplicar em qualquer gráfico D3 futuro que combine hover animado com troca de estado animada no mesmo elemento.

- **2026-08-18**: vigésimo quinto gráfico adicionado (bolhas animadas estilo Gapminder, D3), a partir de [r-graph-gallery.com/271](https://r-graph-gallery.com/271-ggplot2-animated-gif-chart-with-gganimate.html) — a técnica original usa `gganimate::transition_time()` pra gerar um GIF. **Terceira decisão consciente de técnica diferente na mesma leva** (depois da superfície 3D e, antes dela, do redesign): em vez de instalar `gganimate`+`gifski`+`transformr`, a versão interativa reaproveita só o dado bruto e anima a transição entre anos com `.transition()` do D3 — dá play/pause, régua pra arrastar até qualquer ano e tooltip por item, que um GIF de caminho fixo não oferece. Dados fictícios: 12 planetas fictícios em 4 setores galácticos (`set.seed(2094)`), índice tecnológico/expectativa de vida dos colonos/população crescendo por 20 anos via crescimento composto + ruído acumulado, no lugar do dataset real do Gapminder (países/continentes/PIB per capita) do exemplo original. Miniatura estática em pequenos múltiplos (6 anos amostrados) em vez de um único quadro congelado — reaproveita a segunda técnica da página de origem (lá facetada por continente, aqui por ano), mantendo a ideia de "evolução" legível mesmo parada. Achado real: o botão de play usa `setInterval`, e como qualquer gráfico D3 deste acervo pode ser redesenhado do zero num resize (`mount.ts`), era preciso guardar a referência do timer num lugar que sobrevive ao redesenho e limpá-la explicitamente no início de cada desenho novo — sem isso, redimensionar a janela com o play ligado dobraria a velocidade da animação (dois timers rodando ao mesmo tempo). Detalhe completo no README do gráfico.
- **2026-08-18**: vigésimo quarto gráfico adicionado (superfície 3D de interação, `rgl::persp3d()`), a partir de [r-graph-gallery.com/167](https://r-graph-gallery.com/167-animated-3d-plot-imagemagick.html) — a técnica original é uma animação GIF (quadros PNG empilhados via ImageMagick, girando a câmera num caminho fixo). **Decisão consciente de técnica diferente**: em vez de instalar ImageMagick (dependência externa de linha de comando, nunca usada antes neste projeto) e gerar/empilhar dezenas de frames, a versão interativa usa o mesmo padrão `rgl`+`rglwidget()` já estabelecido em [dispersao-3d-cafes](../graficos/correlation/dispersao-3d-cafes) — controle de rotação de verdade (qualquer ângulo, zoom) em vez de uma animação pré-gravada num caminho fixo. Dados fictícios: rendimento de uma safra em função de fertilizante e irrigação com termo de interação (`set.seed(714)`), no lugar da superfície matemática sem contexto do exemplo original. Paleta `RColorBrewer::brewer.pal(9,"YlOrRd")` mapeada pela altura da superfície. **Dois achados reais no caminho**, ambos no README do gráfico: (1) um ângulo de câmera customizado (`view3d()`) mal escolhido deixava a superfície quase de perfil, escondendo a curvatura de interação inteira e fazendo o sombreamento especular padrão do `rgl` escurecer a superfície quase até preto — corrigido voltando ao ângulo *padrão* do `rgl` (sem `view3d()`) e com `specular = "black"`; (2) `cut()` aplicado a uma matriz de altura devolve um vetor simples, perdendo a dimensão original — a matriz de cores precisa ser reconstruída explicitamente com `matrix(paleta[faixa], nrow = nrow(z))` depois.
- **2026-08-18**: vigésimo terceiro gráfico adicionado (radar/spider chart com múltiplos grupos, `fmsb`), a partir de [r-graph-gallery.com/143](https://r-graph-gallery.com/143-spider-chart-with-saveral-individuals.html). A página tinha 3 variações progressivas (básica, customizada com legenda/cores, limites automáticos) que eram passos de tutorial do mesmo gráfico, não técnicas alternativas — combinadas na versão mais completa (cores + legenda customizadas, eixo com limite explícito 0–20 em vez de automático, mais didático pra quem vê o gráfico pela primeira vez). Dados fictícios: três personagens de RPG (Guerreiro, Mago, Ladino) com valores autorais em 5 atributos (Força, Inteligência, Destreza, Vitalidade, Carisma), pensados pra cada um ter um perfil reconhecível, no lugar dos "mister-a/b/c" e variáveis sem nome do exemplo original. Paleta `RColorBrewer::brewer.pal(3, "Set2")` no lugar das cores `rgb()` manuais do exemplo. Primeira versão interativa deste gráfico — `fmsb` é grafismo base do R sem widget pronto — construída direto em D3 seguindo o padrão pós-migração (`script.R` exporta só o dado bruto em `data.json`, o D3 recalcula a geometria sozinho, já que a grade de um radar chart é regular o bastante pra não divergir do estático). **Achado real no caminho**: `fmsb::radarchart()` desenha os eixos em sentido anti-horário a partir do topo (confirmado comparando o `output.png` com o cálculo manual dos ângulos) — o oposto do sentido horário usado no circular barplot deste mesmo acervo; a versão D3 precisou inverter o sinal do incremento angular pra bater com a estática. Detalhe completo no README do gráfico, seção "Possíveis problemas pelo caminho".
- **2026-08-14**: **redesign visual total do site**, a pedido explícito do usuário ("faça o redesign TOTAL no site, deve ficar irreconhecível e ser algo totalmente diferente"), usando a skill `impeccable` (fluxo `new-work` completo: interview, roll de direção via `concept-seed.mjs`, confirmação do usuário contra 3 desafiantes do catálogo). Trocou o mundo visual inteiro — de "site de produto com cor por categoria" pra **"caderno de campo"**: um caderno de laboratório/naturalista, com duas superfícies (papel quadriculado pra ler, mural de cortiça pra navegar), categoria virando fita washi em vez de bloco de fundo, tipografia nova (Bitter + Public Sans + Courier Prime + Caveat, nenhuma reaproveitada do sistema anterior), inclinação determinística por hash do id em cada espécime (`lib/tilt.ts`), e numeração cronológica de entrada (`Nº 0XX`) na página de cada gráfico. Sistema completo documentado em [DESIGN.md](../DESIGN.md). Achado de caminho relevante: uma textura de ruído via SVG `feTurbulence` em `background-image` (tentada nas duas superfícies pra dar grão de papel/cortiça) causava um bug de repintura do Chromium em páginas longas — blocos inteiros do grid quadriculado sumiam ao rolar, de forma intermitente. Diagnosticado isolando a camada via `getComputedStyle`/JS e comparando screenshots antes/depois; removida em favor de gradientes simples (`repeating-linear-gradient`/`radial-gradient`), que não têm o problema. Sem subagentes `impeccable-finish-reviewer`/`impeccable-documenter` disponíveis neste harness — revisão final e DESIGN.md feitos em thread pelo mesmo agente, com screenshots via Chrome real (`claude-in-chrome`), não o pane de preview interno (não composita frames de forma confiável, ver "Verificação visual precisa de Chrome real" nas memórias de sessão).
- **2026-07-30**: vigésimo segundo gráfico adicionado (dendrograma interativo colapsável, `collapsibleTree`), a partir de [r-graph-gallery.com/336](https://r-graph-gallery.com/336-interactive-dendrogram-with-collapsibletree.html) — página com uma única técnica (não teve variação pra perguntar ao usuário). **Exceção pontual à regra de sempre trocar dado/paleta**, a pedido explícito do usuário pra este gráfico: usa o dataset real `warpbreaks` (embutido no R) e a cor padrão do pacote, iguais ao exemplo de referência — regra geral do acervo continua valendo pros próximos gráficos. Só existe como widget interativo (sem equivalente `ggplot2`), então `output.png` vem de um screenshot do próprio `widget.html` via `webshot2`, garantindo igualdade visual entre as duas versões por construção. Dois achados de caminho, ambos detalhados em [AGENTS.md](../AGENTS.md) "Lições aprendidas": (1) o screenshot saía com uma franja de cor por letra nos rótulos — artefato do anti-aliasing do Chromium headless em fonte pequena, não algo que aparece no widget real, corrigido com `zoom = 2` no `webshot2::webshot()`; (2) o wrapper `.collapsibleTree` tem o mesmo bug de sizing fixo (960×500) já visto em `ggiraph`/`streamgraph`, corrigido só acrescentando a classe à lista de seletores do fix genérico já existente em `[slug].astro`.
- **2026-07-30**: vigésimo primeiro gráfico adicionado (circular barplot empilhado, primeira entrada da categoria nova `ranking`), a partir de [r-graph-gallery.com/299](https://r-graph-gallery.com/299-circular-stacked-barplot.html). A página-índice tinha 4 variações progressivas (básico, labels, gap/grupos/ordenação) mais essa versão final combinando todas as técnicas — usuário escolheu, via pergunta explícita, seguir direto a versão final/mais completa. Dados fictícios: rede de livrarias com filiais agrupadas por região (Norte/Sul/Leste/Oeste, quantidades diferentes por região) e receita mensal fictícia em 3 linhas de produto (ficção/não-ficção/infantil), `set.seed(1806)`, no lugar dos indivíduos genéricos ("Mister N") e variáveis sem nome do exemplo original; paleta `RColorBrewer::brewer.pal(3, "Dark2")` no lugar do `scale_fill_viridis()`. Uma primeira versão interativa via `plotly::plot_ly(type = "barpolar")` foi feita e depois **removida** ainda na mesma sessão: o usuário comparou o card da galeria (que usa o `output.png`) com a página do widget e notou que o `barpolar` nativo do plotly não reproduz o grid customizado, os rótulos girados nem o arco por grupo do estático — visualmente é outro gráfico, não só outra paleta. Decisão registrada como regra nova: paridade visual entre `output.png` e `widget.html` (não só de cor) é obrigatória, e na falta dela é preferível ficar só com o estático (`interactive: false`) a publicar um widget que diverge do preview. Ver memória de sessão "paridade visual estático/interativo".
- **2026-07-29**: **três gráficos corrigidos pelo mesmo motivo** — usuário notou que a versão interativa não batia com a miniatura do card (o `output.png`) em [rede-densa-hairball](../graficos/network/rede-densa-hairball), [rede-direcionada-ponderada](../graficos/network/rede-direcionada-ponderada) e [circle-packing-hierarquico](../graficos/part-of-whole/circle-packing-hierarquico). Causa comum: pacotes de widget não têm o sistema de escalas do `ggplot2`, então o que no estático é uma linha de `scale_*` precisa ser calculado à mão e passado pronto — e nos três casos havia um valor fixo no lugar de um gradiente. Correções: no hairball, cor por nó reproduzindo a rampa exata do `scale_colour_distiller("OrRd")`, mais cor/opacidade de aresta iguais às do estático; na rede direcionada, opacidade por aresta embutida em `rgba()` (o `visNetwork` só tem opacidade global) e borda do nó igual ao fundo; no circle packing, camada de CSS+JS por cima do widget porque o `circlepackeR` não permite uma cor por nível (limitação detalhada em [AGENTS.md](../AGENTS.md)), além de centralizar o desenho. Em todos, a paleta passou a viver numa variável só, alimentando as duas versões. Verificação feita capturando os widgets com `webshot2` e comparando com os PNGs — antes disso a checagem por DOM/canvas dava falso "está certo", porque confirmava que o widget respeitava o próprio código sem revelar que o código divergia do estático. De caminho: descoberto que esta máquina não tem Chrome (só Edge) pro `webshot2`, ver [SETUP.md](SETUP.md). Outro achado: o layout `circlepack` do `ggraph` é **estocástico** (duas chamadas seguidas com os mesmos dados dão arranjos diferentes), então o `output.png` mudava a cada execução — corrigido com `set.seed()` antes do layout, e conferido rodando o script duas vezes e comparando o hash do PNG. Achado de quebra durante essa verificação: o `circlepackeR` às vezes renderizava um SVG `0x0` (widget invisível, e o iframe da página zerado junto), de forma intermitente e sem erro no console, porque ele mede o container uma vez só e tem o handler de `resize` vazio — o mesmo `<script>` da paleta agora refaz o desenho quando isso acontece. Detalhe completo em [AGENTS.md](../AGENTS.md).
- **2026-07-29**: pendência aberta (não resolvida): no navegador do usuário, o widget de [circle-packing-simples](../graficos/part-of-whole/circle-packing-simples) continua aparecendo cortado com barra de rolagem, mesmo depois da mudança de proporção pra 3:2 e de reiniciar o servidor. No ambiente de teste (Edge/preview, viewport 1280x900) o widget mede 512px de altura e cabe inteiro, então falta pista do lado do usuário — qual navegador, nível de zoom e altura de janela — pra reproduzir. Retomar quando houver essa informação.

- **2026-07-29**: vigésimo gráfico adicionado (circle packing hierárquico, `ggraph`+`circlepackeR`), segundo e último dos dois gráficos vindos da página-índice [r-graph-gallery.com/circle-packing](https://r-graph-gallery.com/circle-packing.html) — este combina as variações da família "múltiplos níveis" (básico → cor por profundidade → esconder o primeiro nível) com a técnica mais completa e limpa das três: grafo em árvore (`graph_from_data_frame`) + `layout="circlepack"` do `ggraph`, colorindo por profundidade e escondendo a raiz (cor branca igual ao fundo). Duas páginas de variação adicionais da mesma família (rotular nós específicos) retornaram 404 no site de origem — seguido só o que pôde ser verificado. Interativo via `circlepackeR` (zoom por clique, primeira vez que este pacote é usado no acervo), que exige os dados no formato `data.tree` em vez de grafo — convertidos a partir da mesma tabela original via `pathString`, sem duplicar dado. Dados fictícios: catálogo de streaming (Filmes/Séries/Música/Podcasts → 3 gêneros cada → 2 títulos cada, horas assistidas/ouvidas inventadas) no lugar da hierarquia de pacotes de software (`flare`) do exemplo original. Detectado e corrigido no caminho: o layout `circlepack` exige `0` (não `NA`) como valor nos nós não-folha; e o widget `circlepackeR` calcula seu próprio diâmetro como o menor entre largura/altura do container, então `width`/`height` explícitos evitam depender do padrão. A animação de zoom ao clicar não pôde ser confirmada automaticamente nesta sessão (mesma limitação de `requestAnimationFrame`/`.transition()` no pane de preview já registrada em [AGENTS.md](../AGENTS.md) "Lições aprendidas") — a estrutura, os dados e o tamanho do widget foram verificados, mas a suavidade do zoom em si depende de conferência manual do usuário.
- **2026-07-29**: décimo nono gráfico adicionado (circle packing de um nível, `packcircles`+`ggiraph`), primeiro de dois gráficos vindos da mesma página-índice ([r-graph-gallery.com/circle-packing](https://r-graph-gallery.com/circle-packing.html)), que por sua vez linka pra 10 páginas numeradas separadas — o hub tem duas famílias de exemplos (um nível vs. hierárquico), cada uma virando um gráfico deste acervo, **entregues em 2 commits/pushes separados a pedido do usuário**. Este primeiro combina as variações progressivas da família "um nível" (básico → paleta customizada → espaçamento entre bolhas → interativo) numa técnica só: layout via `circleProgressiveLayout()`+`circleLayoutVertices()`, com respiro entre bolhas (raio × 0,95) e versão interativa via `geom_polygon_interactive()`+`girafe()`. Dados fictícios (downloads de 24 jogos indie inventados) no lugar dos rótulos genéricos ("Group a"...) do exemplo original; paleta trocada pra `scale_fill_distiller("Spectral")` no lugar do `scale_fill_viridis()` padrão. Sessão rodou numa máquina nova sem R instalado — ver `docs/SETUP.md` pro caminho de instalação atualizado.
- **2026-07-27**: bug corrigido no gráfico de [rede interativa com networkD3](../graficos/network/rede-interativa-networkd3) — usuário reportou algo estranho na página; investigação achou que o nó `J` estava ausente do grafo (12 nós esperados, só 11 renderizados), silenciosamente, porque `simpleNetwork()` infere os nós só a partir do que aparece nas ligações e, com `set.seed(77)`, a letra "J" nunca saía sorteada em nenhuma das 18 linhas. Corrigido comparando a lista de nós esperados com os presentes e adicionando uma ligação extra pro nó faltante — inicialmente ligado a um nó aleatório, o que criava uma cadeia de pendentes (`A-L-J`) que o layout de forças empurrava pra fora do enquadramento do `output.png`; corrigido de novo ligando ao nó de maior grau (um "hub") em vez de um nó periférico. De caminho, trocado `union()` por `base::union()` explícito (o `igraph`, carregado no mesmo script, mascara essa função).

- **2026-07-27**: décimo oitavo gráfico adicionado (rosca de alocação de tempo, primeira entrada da categoria nova `part-of-whole`), a partir de [r-graph-gallery.com/doughnut-plot](https://r-graph-gallery.com/doughnut-plot.html). A página tinha várias variações (básica, customizada, controle de espessura do anel via `xlim`, alternativa em R base) que eram passos progressivos de tutorial, não estilos alternáveis — combinadas num gráfico só com a técnica mais completa (`geom_rect()`+`coord_polar()`, rótulos, paleta e anel customizados). Dados fictícios (5 categorias de uso do tempo somando 24h) no lugar dos rótulos genéricos do original. Interativo via `plotly::plot_ly(type="pie", hole=...)` em vez de `ggplotly()` — conversão de `coord_polar()` é limitação conhecida do pacote, mesma lógica de "técnica diferente quando não há conversão direta" já usada noutros gráficos do acervo.
- **2026-07-27**: segundo commit do gráfico de dispersão 3D de cafés ([graficos/correlation/dispersao-3d-cafes](../graficos/correlation/dispersao-3d-cafes)) — adicionada a versão interativa (`rgl::rglwidget()`, `interactive: true`), completando o pedido do usuário de entregar este gráfico em 2 commits/pushes separados. Mesma cena/dados do commit estático anterior, sem duplicação. Legenda (de novo sem `legend3d()`, que não funciona headless) virou uma lista HTML simples via `htmltools`, ao lado do widget. Detectado e corrigido nesse commit: o canvas WebGL nasce com largura fixa em pixels e estourava a largura da página — precisou de `max-width: 100%` via CSS pra ficar responsivo (mesmo tipo de problema já resolvido antes no iframe dos widgets, ver "Lições aprendidas" em [AGENTS.md](../AGENTS.md)).
- **2026-07-27**: décimo sétimo gráfico adicionado (dispersão 3D com `rgl`, categoria `correlation`), a partir de [r-graph-gallery.com/3d_scatter_plot](https://r-graph-gallery.com/3d_scatter_plot.html) — **entregue em 2 commits/pushes separados, a pedido do usuário**: este primeiro commit traz só a versão estática (`interactive: false`), a interativa (`rglwidget()`) vem num segundo commit em seguida. Dados fictícios (avaliação sensorial de cafés — acidez/corpo/doçura por torra, `set.seed(3005)`) no lugar do dataset real `iris` (medidas de pétala/sépala por espécie) do original. Pacotes novos: `rgl` (precisa de `options(rgl.useNULL = TRUE)` **antes** de `library(rgl)` pra rodar sem janela gráfica real) e `png` (usado só pra compor a legenda manualmente por cima do `snapshot3d()`, já que `legend3d()` não renderiza em modo headless — ver "Lições aprendidas" em [AGENTS.md](../AGENTS.md)).
- **2026-07-27**: **virada de escopo do site** — deixou de ser log técnico pessoal e passou a ser um acervo autoral de referência sobre tipos de gráfico que dá pra criar, a pedido do usuário. Toda página de gráfico agora segue um padrão fixo: ficha técnica no topo (pacotes, tipo de dado exigido, nível, tags — campos novos e obrigatórios no frontmatter) + 7 seções de texto (o que é / quando usar e evitar / que dados precisa / como ler / como foi feito / possíveis problemas / variações) + o `script.R` renderizado no fim com destaque de sintaxe e botão de copiar. A antiga seção `## Observações` (notas de bastidor) foi aposentada; a parte de "problemas encontrados" virou "possíveis problemas pelo caminho", agora escrita como aviso pra quem vai reproduzir. Nenhum texto renderizado cita a fonte original. Os 16 gráficos existentes foram migrados pro padrão na mesma sessão. Padrão detalhado no passo 6 do [`WORKFLOW.md`](WORKFLOW.md); decisão registrada em [`AGENTS.md`](../AGENTS.md).

- **2026-07-27**: décimo sexto gráfico adicionado (dashboard interativo com mapa coroplético + dispersão + barras via `ggiraph`+`patchwork`, categoria `map`), a partir de [r-graph-gallery.com/414](https://r-graph-gallery.com/414-map-multiple-charts-in-ggiraph.html). A página original tem 6 variações crescentes de interatividade (a primeira, com `mtcars`, não envolve mapa); as outras 5 viraram os botões deste gráfico, a pedido do usuário — mesmo padrão do gráfico de linha interativa com ggiraph. Dados fictícios (investimento em P&D x índice de inovação por país, `set.seed(1414)`) sobre geografia real (`spData::world`), no lugar do PIB x felicidade reais (Gapminder) do original. Pacote novo: `spData` (fornece o mapa-múndi como `sf` sem precisar de shapefile externo). Nesse meio-tempo, uma tentativa anterior (bubble map de terremotos fictícios, também da página 19) foi criada e desfeita na mesma sessão — o usuário na verdade queria replicar o conceito de "múltiplas variações de estilo" da página 414, não um segundo gráfico da página 19.
- **2026-07-27**: décimo quinto gráfico adicionado (bubble map interativo do Brasil com `leaflet`, primeira entrada da categoria nova `map`), a partir de [r-graph-gallery.com/19](https://r-graph-gallery.com/19-map-leafletr.html). Dados fictícios (vendas mensais em R$ mil, `set.seed(2026)`) para 20 capitais/grandes cidades brasileiras no lugar do dataset `quakes` (terremotos perto de Fiji) do original; mapa recentralizado no Brasil. Primeira versão trocou basemap pra `CartoDB.Positron` e paleta pra `BuPu`, mas o usuário não gostou do visual "sem graça" perto do original — revisado ainda na mesma sessão pra manter o basemap de satélite (`Esri.WorldImagery`, igual ao original) e usar `OrRd` no lugar de `BuPu` (mesma família "quente" do `YlOrBr` original, sem copiar a paleta idêntica — ver "Decisões fechadas" em [AGENTS.md](../AGENTS.md)). Pacote novo: `leaflet` (CRAN). Mesmo padrão de thumbnail via `webshot2` dos outros htmlwidgets sem equivalente `ggplot2`.
- **2026-07-24**: arc diagram ([graficos/network/arc-diagram-d3](../graficos/network/arc-diagram-d3)) revertido pra versão original (commit `ef392cb`), a pedido do usuário — as duas ampliações feitas em 2026-07-23 (72 nós + pan/zoom, depois reduzido a 50 nós + clique-pra-fixar) ficaram "muito bugadas" na prática. Voltou a ser 14 nós / 3 grupos / 17 conexões, só com hover (destaca arcos conectados + tooltip), legenda pequena dentro do próprio SVG, sem pan/zoom/clique-pra-fixar — igual à versão publicada originalmente, próxima do [data-to-viz.com/graph/arc](https://www.data-to-viz.com/graph/arc.html). `widget_files/data.js` (introduzido na ampliação, pra evitar duplicar dados entre `script.R`/`widget.html`) removido — a versão restaurada volta a ter os dados hardcoded nos dois lugares, como no commit original.
- **2026-07-24**: décimo quarto gráfico adicionado (série temporal interativa com `dygraphs`, categoria `evolution`), a partir de [r-graph-gallery.com/318](https://r-graph-gallery.com/318-custom-dygraphs-time-series-example.html). Dados 100% fictícios (downloads diários de um app, 300 dias, tendência + sazonalidade semanal) no lugar do CSV real de bicicletas do exemplo original. `dygraphs` é puro htmlwidget sem equivalente `ggplot2`, então `output.png` veio de um screenshot do `widget.html` via `webshot2`, mesmo padrão dos gráficos de rede/streamgraph.
- **2026-07-24**: gráfico de linha interativa com `ggiraph` revisado — em vez de fixar um único tratamento de CSS combinando as técnicas mais úteis, o `widget.html` agora tem uma **barra de botões acima do gráfico** que troca ao vivo entre as 4 variações de interação da página original (hover simples, destacar+apagar outras, hover avançado, tooltip+zoom), a pedido do usuário ("adicione todos os gráficos que tinha na página... faça um gráfico só mas adicione uma barra acima com botões pra alterar o estilo do gráfico abaixo"). Implementado com `htmltools::save_html()` combinando 4 widgets `girafe` (mesmo `plot`/dados, só `girafe_options()` diferente) numa `tagList` só, com JS puro pro toggle — ver README do gráfico.
- **2026-07-24**: décimo terceiro gráfico adicionado (linha interativa com CSS via `ggiraph`, categoria `evolution`), a partir de [r-graph-gallery.com/412](https://r-graph-gallery.com/412-customize-css-in-interactive-ggiraph.html). A página tinha 5 variações de código, mas todas em cima do mesmo gráfico (só o CSS de interação — hover, tooltip, seleção, zoom — mudava entre elas); por pedido explícito do usuário ("se os gráficos forem muito parecidos e as interações mudarem sutilmente, pode colocar na mesma página"), viraram um gráfico só combinando as técnicas mais úteis de cada variação, em vez de uma pasta por variação.
- **2026-07-24**: nono a décimo segundo gráficos adicionados de uma vez, categoria `network`, todos a partir da mesma página ([data-to-viz.com/graph/network](https://www.data-to-viz.com/graph/network.html)), que tem vários demos diferentes em vez de um único gráfico — usuário escolheu, via pergunta explícita, agrupar em 4: **rede direcionada+ponderada** (setas + espessura de linha, une os 2 demos "Directed"), **rede densa hairball** (ilustra o "erro comum" citado na página), **comparação de layouts** (Fruchterman-Reingold vs DrL vs aleatório, 3 painéis via `patchwork`) e **tipos básicos de rede** (não-ponderada vs ponderada, 2 painéis, une os 2 demos "Undirected"). Pacotes novos: `patchwork` (combina múltiplos `ggraph` num único `output.png`) e `visNetwork` (widget interativo com setas nativas + arrastar nós, usado nos 2 primeiros). Ver "Lições aprendidas" em [`AGENTS.md`](../AGENTS.md) pro detalhe do `Rscript` precisar rodar com o working directory dentro da própria pasta do gráfico (senão `ggsave()` salva o PNG na raiz do repo).
- **2026-07-23**: gráfico de arc diagram ajustado de novo, a pedido do usuário — reduzido de 72 nós/6 grupos pra 50 nós/5 grupos (tinha ficado poluído visualmente), adicionado clique-pra-fixar destaque num arco (`.pinned`, persiste sem hover até novo clique ou duplo-clique), e legenda/dica movidas pra fora do SVG (overlay HTML) porque ficavam ilegíveis reescalando junto com o `viewBox` largo da rede. Ver "Lições aprendidas" em [`AGENTS.md`](../AGENTS.md).
- **2026-07-23**: gráfico de arc diagram ampliado bem além do inicial, a pedido do usuário — de 14 nós/17 conexões pra 72 nós em 6 grupos / 115 conexões, e adicionado pan (arrastar) + zoom (scroll) + reset (duplo-clique) no widget D3, já que a rede ficou grande demais pra caber inteira na tela. `script.R` agora exporta os dados (`nodes`/`links`/`groupColor`) pra `widget_files/data.js` via `jsonlite`, fonte única entre a versão estática e a interativa — não editar `data.js` a mão, ele é regenerado a cada `Rscript script.R`.
- **2026-07-23**: oitavo gráfico adicionado (arc diagram, categoria `network`). Primeiro gráfico do projeto com `widget.html` escrito à mão em D3.js puro em vez de gerado por um pacote R — não existe pacote R com arc diagram interativo pronto. `output.png` continua vindo do `ggraph` normalmente, mesmos dados fictícios nas duas versões. Ver "Decisões fechadas" em [`AGENTS.md`](../AGENTS.md).
- **2026-07-23**: sexto e sétimo gráficos adicionados (densidade 2D estática + superfície 3D interativa, categoria `distribution`, mesmo dataset fictício de 4 clusters gaussianos pra ficarem comparáveis). Página original tinha 3 variações (incluindo um scatterplot só ilustrativo, não replicado); usuário pediu as duas versões de densidade como gráficos separados.
- **2026-07-22**: quinto gráfico adicionado (heatmap com clustering hierárquico via `heatmaply`, categoria nova `correlation`). Primeira fonte fora do R Graph Gallery (data-to-viz.com, do mesmo autor); matriz ficticia gerada do zero em vez do CSV real externo do exemplo original.
- **2026-07-22**: quarto gráfico adicionado (sankey diagram com `networkD3::sankeyNetwork()`, categoria nova `flow`). Pedido do usuário: dataset real do exemplo original (~68 nós) fica poluído, então usamos um fluxo fictício bem menor (8 nós). Detectado um comportamento não-óbvio do binding JS do pacote (`color(d.group.replace(/ .*/, ""))`, em `sankeyNetwork.js`) que colore nós pela primeira palavra do nome, não pelo nome inteiro — ver README do gráfico e [`AGENTS.md`](../AGENTS.md) "Lições aprendidas".
- **2026-07-22**: terceiro gráfico adicionado (rede interativa com `networkD3::simpleNetwork()`, categoria `network`). Reaproveitado o padrão de thumbnail via `webshot2` (mesmo widget-sem-equivalente-ggplot2 do streamgraph). Página original tinha duas variações de código (básica/customizada); passou a valer a regra em [`WORKFLOW.md`](WORKFLOW.md) de perguntar ao usuário qual usar antes de seguir.
- **2026-07-22**: segundo gráfico adicionado (streamgraph interativo, categoria nova `evolution`). Pacote `streamgraph` só existe no GitHub (`hrbrmstr/streamgraph`, não está no CRAN); instalado `webshot2`+`chromote` pra gerar thumbnail estático de widgets que não têm equivalente em `ggplot2` (usa o Chrome já instalado na máquina). `pandoc` não está instalado, então `saveWidget()` precisa de `selfcontained = FALSE` — ver [`SETUP.md`](SETUP.md) e [`AGENTS.md`](../AGENTS.md) "Lições aprendidas".
- **2026-07-21**: site da galeria (`site/`, Astro) criado — home com filtro por categoria, página de detalhe por gráfico (widget interativo ou imagem com zoom), paleta editorial própria, deploy configurado via GitHub Actions pra GitHub Pages. Detalhes em [`PRODUCT.md`](../PRODUCT.md) e "Decisões fechadas" em [`AGENTS.md`](../AGENTS.md).
- **2026-07-21**: primeiro gráfico adicionado (hierarchical edge bundling). Corrigido bug de auto-conexão (`from == to`) que quebrava `geom_conn_bundle` — ver [`AGENTS.md`](../AGENTS.md) "Lições aprendidas".
- **2026-07-21**: projeto criado. Ambiente configurado (R 4.6.1 + tidyverse, ggraph, igraph, RColorBrewer — ver [`SETUP.md`](SETUP.md)). Estrutura de pastas e documentação inicial criadas.
